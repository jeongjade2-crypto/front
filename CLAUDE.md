@AGENTS.md

# 프로젝트 개요

대학 교무처 직원 **김똘미**가 사용하는 성적관리 서비스의 프론트엔드를 만든다.

- 이 서비스의 실제 사용자는 개발자가 아닌 교무처 직원이다. 화면은 설명서 없이도 바로 이해하고 쓸 수 있어야 한다.
- 백엔드는 이미 완성되어 운영 중이다. **백엔드는 수정하지 않고, API 호출만 한다.**
- API의 유일한 기준은 Swagger 문서다: **http://lecture.s-mart.kr/api**

# 기술 스택

- Next.js 16 (App Router)
- React 19
- TypeScript
- Tailwind CSS v4
- shadcn/ui
- 배포: Vercel — GitHub `master` 브랜치에 push하면 Vercel이 자동으로 새 버전을 배포한다 (저장소: https://github.com/jeongjade2-crypto/front)

# 반드시 지킬 5가지

1. **모든 코드에 상세한 한글 주석** — `korean-code-comments` 스킬을 항상 적용한다. 파일 맨 위 설명, 함수 위 입출력 설명, 복잡한 부분의 이유 설명이 없는 코드는 미완성으로 간주한다.
2. **API 주소·비밀값 하드코딩 금지** — 코드에 URL이나 키를 직접 적지 않고, `NEXT_PUBLIC_API_BASE_URL` 환경변수(`.env.local`, 예시는 `.env.example`)를 사용한다.
3. **API 경로·요청·응답 형식은 Swagger에서 확인** — http://lecture.s-mart.kr/api 를 직접 확인하고 반영한다. 추측으로 엔드포인트나 응답 구조를 만들어내지 않는다.
4. **내 확인 없이 git push·배포·패키지 대량 설치·파일 삭제 금지** — 이 네 가지는 실행 전에 반드시 사용자에게 알리고 승인을 받는다.
5. **사용자에게 보이는 메시지는 전부 한글 + 다음 행동 안내** — 에러/안내/완료 메시지는 쉬운 한글로 쓰고, 사용자가 다음에 무엇을 하면 되는지 함께 알려준다.

# AI팀 작업순서

새 기능은 아래 순서로 서브 에이전트를 거쳐 완성한다. 각 에이전트는 자기 산출물만 책임지고, 다음 단계는 앞 단계 결과물을 확인한 뒤 시작한다.

1. **requirements-analyst** — 새 기능 요청이 들어오면 가장 먼저 실행한다. 코드는 쓰지 않고, `docs/specs/features.md`에 기능 명세서(완료 조건 3개 이상, "~하면 ~가 보인다" 형식)를 작성한다.
2. **design-architect** — 명세서가 준비되면 실행한다. 코드는 쓰지 않고, Swagger(http://lecture.s-mart.kr/api)를 참고해 `docs/specs/design.md`에 화면·파일을 어떤 순서로 만들지 설계한다.
3. **ui-ux-expert** / **frontend-developer** — 설계도대로 실제 화면과 코드를 만든다.
   - 모든 화면은 PC와 모바일 웹 모두에서 잘 보이는 반응형으로 만든다.
   - 버튼·안내문 등 사용자에게 보이는 문구는 쉬운 한글로 쓴다.
   - 색·글자 크기 규칙은 `docs/design.md` 한 곳에서만 관리하고, 컴포넌트에 값을 직접 하드코딩하지 않는다.
   - 저장·삭제 동작 전에는 반드시 확인창을 띄운다.
   - 파일 삭제나 패키지 대량 설치가 필요하면 먼저 사용자에게 확인받는다("반드시 지킬 5가지" 4번).
   - 이 두 에이전트는 git 명령을 쓰지 않는다.
4. **release-manager** — 완성된 코드를 검토하고 커밋한다. **push나 배포 전에는 반드시 사용자에게 변경 요약을 보여주고 승인을 받는다.** 이 프로젝트에서 git 명령(add/commit/push 등)은 이 에이전트만 사용한다.

# 자주 쓰는 명령

- `npm run dev` — 개발 서버 실행
- `npm run build` — 커밋 전 반드시 통과해야 한다
- `npm run lint` — 코드 스타일·오류 검사
